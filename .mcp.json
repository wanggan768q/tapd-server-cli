{
  "mcpServers": {
    "tapd": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "tapd-server-cli@~0.2.0"],
      "env": {
        "TAPD_TOKEN": "${user_config.tapd_token}",
        "TAPD_LOG_LEVEL": "info"
      }
    }
  }
}
