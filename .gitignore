node_modules/
dist/
.env
.env.local
*.log
.DS_Store
.vitest-cache/
coverage/
*.tsbuildinfo

# Client-side local configs (per-developer)
.claude/
.codex/
.opencode/

# OMC runtime state (session / plan / memory caches)
.omc/

# Installer backups created during local dry-run / testing
*.bak.*

# Local sample artifacts (real downloaded attachments etc.)
dist-e2e-*

# Secrets fallback location
.tapd-token
