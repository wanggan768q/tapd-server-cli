# Plugin E2E Smoke — 2026-05-28

> **目的**：闭环 issue #10。把 PR #1 / #11 / #12 累积承诺过的所有用户路径**实际跑一遍**并贴证据。
>
> **结构**：8 条 checklist。**自动化部分**（标 🤖）跑 `bash scripts/smoke/run-plugin-e2e.sh` 一键完成；**手工部分**（标 👤）必须人在 Claude Code GUI 里实操，把 stdout / 截图 / 错误贴到对应「证据」区块。

## 环境

| 项 | 值 |
|---|---|
| 仓库 commit | `aae7a0008abd8ed5af3f544bda36f5d22b8351e5`（chore/smoke-e2e）|
| 包版本 | `0.2.0` |
| `.mcp.json` 锁定 | `tapd-server-cli@~0.2.0`（自动跟 patch） |
| 测试人 | wanggan768q |
| 测试时间 | 2026-05-28T09:47:11Z |
| Claude Code 版本 | `2.1.150 (Claude Code)` |
| OS | Windows 11（MINGW64_NT-10.0-26200，Git Bash） |

## 自动化部分（🤖 5 项 — 在终端跑一行）

```bash
SMOKE_OUT=docs/smoke/2026-05-28-plugin-e2e.log bash scripts/smoke/run-plugin-e2e.sh
```

预期 stdout 末尾出现 `=== 汇总：PASS=5  FAIL=0  SKIP=0 ===`（如果 `claude` CLI 不在 PATH，step 2 SKIP；这是允许的）。

> 参考：`docs/smoke/2026-05-28-plugin-e2e.sample-output.txt` 是本仓库作者第一次跑通的输出样本，可对照确认每一步的格式。`*.log` 默认在 `.gitignore` 里，每次跑不会污染工作区。

### 证据：自动化部分

```
=== Plugin E2E Smoke (commit aae7a00) ===
=== 时间：2026-05-28T09:47:11Z ===

[1/5] plugin / marketplace / .mcp.json 合规
  ✓ 版本三处一致：0.2.0

[2/5] claude plugin validate
  ✓ claude plugin validate 通过

[3/5] npm pack --dry-run 排除 plugin 文件
  ✓ npm 包仅含 dist/ + README + LICENSE + package.json

[4/5] .mcp.json 锁定到 ~<minor>.0
  ✓ .mcp.json args[1] = tapd-server-cli@~0.2.0

[5/5] 验证 ~<minor>.0 范围在 npm registry 上能解析到合法版本
  ✓ 范围 ~0.2.0 解析到 0.2.0（minor 与本仓库一致）

=== 汇总：PASS=5  FAIL=0  SKIP=0 ===

✅ 自动化部分全过。GUI / 交互验证（/plugin install 弹窗、/mcp Connected、
   /tapd-server-cli:login 浏览器弹窗、/tapd-server-cli:update 渲染等）
   请按 docs/smoke/<date>-plugin-e2e.md 的 checklist 手工补证据。

```

退出码：`0`

---

## 手工部分（👤 8 项 — Claude Code GUI 内一项一项做）

### Item 1：`/plugin marketplace add wanggan768q/tapd-server-cli`

**步骤**：

1. 在干净的 Claude Code 会话（没装过本 plugin 的环境）运行 `> /plugin marketplace add wanggan768q/tapd-server-cli`
2. 等待 marketplace 拉取完成

**预期**：

- 命令成功，输出包含 marketplace 名 `tapd-server-cli` 和 plugin 列表（至少含 `tapd-server-cli`）
- 不报错

**证据**：

```text
> 把 Claude Code 会话里的实际输出贴在这里
```

或截图：

> ![](path/to/screenshot-1.png)

---

### Item 2：`/plugin install tapd-server-cli@tapd-server-cli` + 弹窗收 PAT

**步骤**：

1. 接 Item 1 后运行 `> /plugin install tapd-server-cli@tapd-server-cli`
2. 弹窗要求输入 `tapd_token`，输入一个真实可用的 TAPD PAT
3. 按 Enter 提交

**预期**：

- 弹窗提示字段名是 `tapd_token`，描述提到「TAPD 个人访问令牌」
- 输入框 mask 字符（不明文显示）—— 因为 manifest 里 `sensitive: true`
- 提交后输出 `tapd-server-cli@0.2.0 installed`

**证据**：

```text
> 输出 + 截图
```

PAT 是否被 mask？_是 / 否_

---

### Item 3：`/mcp` 显示 `tapd ✓ Connected`

**步骤**：

1. 接 Item 2 后立即运行 `> /mcp`

**预期**：

- 列表里有一行 `tapd` 且状态是 `Connected`（绿色 ✓）
- 工具数 ≥ 5（至少 `whoami` / `list_workspaces` / `list_capabilities` / `update` / `login`）

**证据**：

```text
> /mcp 输出
```

工具数：_填_

---

### Item 4：调 `tapd.whoami` 返回正确身份

**步骤**：

1. 在会话里说："调用 tapd.whoami 看下我是谁"
2. Claude 应自动调 `tapd.whoami` 工具

**预期**：

- 返回的 `user_id` / `user_name` 与你 PAT 对应的 TAPD 账号一致
- `token_preview` 是脱敏形态（如 `b572***1f73`），不是完整 PAT

**证据**：

```json
{
  "user_id": "...",
  "user_name": "...",
  "token_preview": "..."
}
```

token_preview 是否脱敏？_是 / 否_

---

### Item 5：`/tapd-server-cli:login` 弹浏览器 + 工具热加载

**步骤**：

1. 在会话里运行 `> /tapd-server-cli:login`
2. Claude 应调 `tapd.login` 工具，弹出独立 Chrome / Edge 窗口
3. 在弹窗里完成 TAPD 登录
4. 等 Claude 报告"已装配附件下载工具"

**预期**：

- 浏览器窗口确实弹出（Cookie 模式）
- 登录后 cookie 写到 `~/.config/tapd-mcp/cookie`，权限 600（POSIX）
- 工具列表通过 `tools/list_changed` 通知热加载，再调 `/mcp` 时 `tapd` 工具数比 Item 3 多（多了 `tapd.attachments.download` 等）

**证据**：

```text
> /tapd-server-cli:login 输出
> 浏览器弹窗截图
> 登录后 /mcp 工具数对比 Item 3
```

热加载是否生效？_是 / 否（如否，说明客户端版本是否支持 tools/list_changed）_

---

### Item 6：`/tapd-server-cli:update`（PR #12 新增，本轮重点验证）

**步骤**：

1. 在会话里运行 `> /tapd-server-cli:update`
2. Claude 应调 `tapd.update` 工具

**预期**：

- 返回 `current === '0.2.0'`、`latest` 是 npm 上的当前最高版本
- `comparison` 是 `up-to-date`（如果 npm 上还没发更新版）或 `update-available`
- `installed_via === 'plugin'`（因为通过 plugin 安装的）
- 如果有 update，`upgrade_commands[0].label` 提到「Claude Code plugin」、`steps` 含 `/plugin marketplace update`
- Claude 把这堆数据渲染成人类可读的对话回答

**证据**：

```json
{
  "current": "0.2.0",
  "latest": "...",
  "comparison": "...",
  "installed_via": "plugin",
  "upgrade_commands": [...]
}
```

Claude 渲染后的回答：

```text
> Claude 给用户的实际回复
```

`installed_via` 检测正确？_是 / 否_

---

### Item 7：npx 路径走 CLI 优先 + fallback 双场景

**步骤（场景 A — `claude` CLI 在 PATH）**：

```bash
# 在终端
which claude              # 期望路径非空
npx -y tapd-server-cli install claude-code
# 输入 PAT
```

**步骤（场景 B — 临时不让脚本看到 claude）**：

```bash
PATH="/__nonexistent__:$(echo $PATH | tr ':' '\n' | grep -v claude | tr '\n' ':')" \
  npx -y tapd-server-cli install claude-code
# 输入 PAT
```

**预期**：

- 场景 A：stdout 出现 `已通过官方 CLI 注册 Claude Code：<via claude mcp add-json --scope user>`
- 场景 B：fallback 到手写 `~/.claude.json`，**stderr** 出现 `(Claude Code CLI 不可用或失败，降级走手写文件：...)`，stdout 出现汇总行 `✔ claude-code  ~/.claude.json  (via fallback: ...)`

**证据**：

场景 A：
```
> 完整 stdout + stderr
```

场景 B：
```
> 完整 stdout + stderr
```

---

### Item 8：Codex 客户端等价验证

**步骤**：

```bash
npx -y tapd-server-cli install codex
# 输入 PAT
```

然后启动 codex，验证 `mcp_servers.tapd` 已加进 `~/.codex/config.toml`。

**预期**：

- TOML 文件包含 `[mcp_servers.tapd]` 节
- `command = "npx"`、`args = ["-y", "tapd-server-cli"]`、`env.TAPD_TOKEN = "<your-pat>"`
- 在 Codex 会话里能调 `tapd.whoami` 拿到身份

**证据**：

```toml
# ~/.codex/config.toml 相关片段
```

```
> Codex 调用 tapd.whoami 的输出
```

---

## 汇总

| Item | 状态 |
|---|---|
| 🤖 自动化 5 项 | _PASS / FAIL_ |
| 👤 1. marketplace add | _PASS / FAIL_ |
| 👤 2. plugin install + PAT | _PASS / FAIL_ |
| 👤 3. /mcp Connected | _PASS / FAIL_ |
| 👤 4. tapd.whoami | _PASS / FAIL_ |
| 👤 5. login + 热加载 | _PASS / FAIL_ |
| 👤 6. **update（PR #12）** | _PASS / FAIL_ |
| 👤 7. npx CLI prefer + fallback | _PASS / FAIL_ |
| 👤 8. codex 客户端 | _PASS / FAIL_ |

**结论**：_全过 / 部分失败（列出哪些）_

跑完后请把这份 Markdown 链到 issue #10 评论里关闭它。如果有 FAIL，建议直接以 FAIL 为输入开 follow-up bug issue 或评论让我跟进。
